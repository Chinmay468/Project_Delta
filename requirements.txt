requests
python-dotenv
google-auth-oauthlib
google-api-python-client
edge-tts
faster-whisper
opencv-python
# mediapipe: prebuilt binary wheels are available up to Python 3.12.
# On Python 3.13+, scripts/face_crop.py automatically falls back to OpenCV DNN (cv2.dnn) or Haar Cascade detection.
mediapipe; python_version < "3.13"
